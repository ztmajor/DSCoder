# dsh-usage-quota bundle patch: mounts the plugin into a profile layer stack.
# The plugin row id is 'dsh-usage-quota' — users may override its behavior in
# their profile's cordis.patch.yml with a row carrying the same id.
- insert:
    - id: dsh-usage-quota
      name: 'dsh-usage-quota'
