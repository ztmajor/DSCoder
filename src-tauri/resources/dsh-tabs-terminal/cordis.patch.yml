# dsh-tabs-terminal bundle patch: mounts the plugin into a profile layer stack.
# The plugin row id is 'dsh-tabs-terminal' — users may override its behavior in
# their profile's cordis.patch.yml with a row carrying the same id.
- insert:
    - id: dsh-tabs-terminal
      name: 'dsh-tabs-terminal'
